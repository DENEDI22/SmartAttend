---
phase: 12-late-threshold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/system_setting.py
  - app/models/__init__.py
  - app/models/schedule_entry.py
  - app/routers/admin.py
  - app/templates/admin_base.html
  - app/templates/admin_settings.html
  - app/templates/admin_devices.html
autonomous: true
requirements: [LATE-01, LATE-02]

must_haves:
  truths:
    - "Admin can set a global default late threshold in minutes from a settings page"
    - "Admin can set a per-schedule-entry late threshold override when adding a schedule entry"
    - "Global default persists across server restarts (stored in DB)"
    - "Per-entry override is nullable (NULL means use global default)"
  artifacts:
    - path: "app/models/system_setting.py"
      provides: "SystemSetting key-value model"
      contains: "class SystemSetting"
    - path: "app/templates/admin_settings.html"
      provides: "Admin settings page with late threshold form"
      contains: "late_threshold"
    - path: "app/models/schedule_entry.py"
      provides: "ScheduleEntry with nullable late_threshold_minutes column"
      contains: "late_threshold_minutes"
  key_links:
    - from: "app/routers/admin.py"
      to: "app/models/system_setting.py"
      via: "DB query to read/write global threshold"
      pattern: "SystemSetting"
    - from: "app/routers/admin.py"
      to: "app/models/schedule_entry.py"
      via: "schedule_add saves late_threshold_minutes from form"
      pattern: "late_threshold_minutes"
---

<objective>
Create the data layer and admin UI for configuring late thresholds: a SystemSetting model for the global default, a nullable column on ScheduleEntry for per-entry overrides, an admin settings page, and the schedule form field.

Purpose: Enables admins to configure when a student is considered "late" -- the foundation for late classification in Plan 02.
Output: SystemSetting model, admin settings page, ScheduleEntry column, schedule form field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-late-threshold/12-CONTEXT.md

@app/models/schedule_entry.py
@app/models/__init__.py
@app/routers/admin.py
@app/templates/admin_base.html
@app/templates/admin_devices.html
@app/database.py

<interfaces>
From app/models/schedule_entry.py:
```python
class ScheduleEntry(Base):
    __tablename__ = "schedule_entries"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    device_id: Mapped[int] = mapped_column(Integer, ForeignKey("devices.id"), nullable=False)
    teacher_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    class_name: Mapped[str] = mapped_column(String, nullable=False)
    weekday: Mapped[int] = mapped_column(Integer, nullable=False)
    start_time: Mapped[time] = mapped_column(Time, nullable=False)
    end_time: Mapped[time] = mapped_column(Time, nullable=False)
```

From app/database.py:
```python
from sqlalchemy.orm import DeclarativeBase
class Base(DeclarativeBase): pass
engine = create_engine(...)
SessionLocal = sessionmaker(...)
def get_db(): ...
```

Admin router pattern: Form POST -> validate -> db.add/commit -> RedirectResponse with ?msg= or ?error= query param.
Admin templates extend admin_base.html with {% block admin_content %}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SystemSetting model, add ScheduleEntry column, wire imports</name>
  <read_first>
    - app/models/schedule_entry.py
    - app/models/__init__.py
    - app/database.py
  </read_first>
  <files>app/models/system_setting.py, app/models/schedule_entry.py, app/models/__init__.py</files>
  <action>
1. Create `app/models/system_setting.py` with a `SystemSetting` model (per D-03):
   - `__tablename__ = "system_settings"`
   - `key: Mapped[str] = mapped_column(String, primary_key=True)` (key-value design)
   - `value: Mapped[str] = mapped_column(String, nullable=False)`
   - Add a class method `get_value(db, key, default=None)` that queries by key and returns value or default
   - Add a class method `set_value(db, key, value)` that upserts (merge) the key-value pair
   - Use SQLAlchemy 2.0 Mapped[] style matching existing models

2. Add nullable column to `app/models/schedule_entry.py` (per D-04):
   - `late_threshold_minutes: Mapped[int | None] = mapped_column(Integer, nullable=True, default=None)`
   - Import `Optional` from typing if needed, or use `int | None` union type

3. Update `app/models/__init__.py`:
   - Add `from app.models.system_setting import SystemSetting` alongside existing imports
   - This ensures the model is registered with Base.metadata for table creation
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && python -c "from app.models.system_setting import SystemSetting; from app.models.schedule_entry import ScheduleEntry; print('OK:', hasattr(SystemSetting, 'key'), hasattr(ScheduleEntry, 'late_threshold_minutes'))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class SystemSetting" app/models/system_setting.py
    - grep -q "late_threshold_minutes" app/models/schedule_entry.py
    - grep -q "system_setting" app/models/__init__.py
    - grep -q "get_value" app/models/system_setting.py
    - grep -q "set_value" app/models/system_setting.py
  </acceptance_criteria>
  <done>SystemSetting model exists with get_value/set_value helpers. ScheduleEntry has nullable late_threshold_minutes column. Both importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin settings page and schedule form late threshold field</name>
  <read_first>
    - app/routers/admin.py
    - app/templates/admin_base.html
    - app/templates/admin_devices.html
  </read_first>
  <files>app/routers/admin.py, app/templates/admin_settings.html, app/templates/admin_base.html, app/templates/admin_devices.html</files>
  <action>
1. Add admin settings routes to `app/routers/admin.py` (per D-01, D-02):
   - Import `SystemSetting` from `app.models.system_setting`
   - Add `GET /admin/settings` route:
     - Read current value: `SystemSetting.get_value(db, "late_threshold_minutes", "10")`
     - Render `admin_settings.html` with `late_threshold_minutes` and `active_page="settings"`
   - Add `POST /admin/settings` route:
     - Accept `late_threshold_minutes: int = Form(...)` 
     - Validate it is a positive integer (1-120 range)
     - Call `SystemSetting.set_value(db, "late_threshold_minutes", str(value))`
     - `db.commit()`
     - Redirect to `/admin/settings?msg=Einstellungen+gespeichert.`

2. Create `app/templates/admin_settings.html`:
   - Extends `admin_base.html`
   - Title: "Einstellungen -- SmartAttend"
   - Block admin_content with:
     - `<hgroup><h1>Einstellungen</h1><p>Globale Systemeinstellungen</p></hgroup>`
     - `<article>` containing:
       - `<h3>Verspaetung</h3>`
       - `<form method="post" action="/admin/settings">`
       - `<label>Standard-Verspaetungsschwelle (Minuten)<input type="number" name="late_threshold_minutes" value="{{ late_threshold_minutes }}" min="1" max="120" required></label>`
       - `<small>Gilt fuer alle Stunden ohne individuelle Einstellung. Schueler, die nach Unterrichtsbeginn + Schwelle einchecken, werden als "Verspaetet" markiert.</small>`
       - `<button type="submit">Speichern</button>`

3. Update `app/templates/admin_base.html` nav:
   - Add settings link between "Benutzer" and the logout form:
     `<li><a href="/admin/settings" {% if active_page == 'settings' %}aria-current="page"{% endif %}>Einstellungen</a></li>`

4. Update schedule form in `app/templates/admin_devices.html` (per D-05):
   - In the "Eintrag hinzufuegen" form, after the 3-column grid (Tag/Startzeit/Endzeit), add:
     ```html
     <label>
       Verspaetung (Min.) <small>-- leer = globaler Standard</small>
       <input type="number" name="late_threshold_minutes" min="1" max="120" placeholder="Standard">
     </label>
     ```
   - This field is optional (no `required` attribute)

5. Update `schedule_add` route in `app/routers/admin.py` (per D-04):
   - Add parameter: `late_threshold_minutes: int | None = Form(None)`
   - When creating `ScheduleEntry`, pass `late_threshold_minutes=late_threshold_minutes`
   - Handle empty string from form: if the form sends empty string, convert to None

6. Update schedule table display in `admin_devices.html`:
   - In the schedule table for each device, add a "Verspaetung" column header after "Zeitraum"
   - Display `{{ entry.late_threshold_minutes or 'Standard' }}` for each row
   - Pass `late_threshold_minutes` in the enriched schedule data in the admin devices route
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && python -c "
from app.routers.admin import router
routes = [r.path for r in router.routes]
assert '/settings' in routes or '/admin/settings' in routes or any('settings' in r for r in routes), f'No settings route: {routes}'
print('Routes OK')
" && grep -q "late_threshold_minutes" app/templates/admin_settings.html && grep -q "Einstellungen" app/templates/admin_base.html && grep -q "late_threshold_minutes" app/templates/admin_devices.html && echo "All template checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "admin/settings" app/routers/admin.py
    - grep -q "late_threshold_minutes" app/templates/admin_settings.html
    - grep -q "Einstellungen" app/templates/admin_base.html
    - grep -q "late_threshold_minutes" app/templates/admin_devices.html
    - grep -q "late_threshold_minutes" app/routers/admin.py
    - grep -q "SystemSetting" app/routers/admin.py
  </acceptance_criteria>
  <done>Admin settings page at /admin/settings shows and saves global late threshold. Schedule add form has optional late_threshold_minutes field. Nav links to settings page. Schedule table shows per-entry threshold.</done>
</task>

</tasks>

<verification>
1. Start server, login as admin, navigate to /admin/settings -- see form with default value 10
2. Change value to 15, save -- see success message, value persists on reload
3. Navigate to /admin/devices, add schedule entry with late_threshold_minutes=5 -- entry saved
4. Add another entry without late_threshold_minutes -- saved with NULL (shows "Standard" in table)
</verification>

<success_criteria>
- SystemSetting model exists and stores key-value pairs in DB
- Admin settings page renders at /admin/settings with late threshold field
- Global default is 10 minutes (per D-02)
- ScheduleEntry has nullable late_threshold_minutes column
- Schedule add form includes optional late threshold field
- Admin nav includes "Einstellungen" link
</success_criteria>

<output>
After completion, create `.planning/phases/12-late-threshold/12-01-SUMMARY.md`
</output>
