---
phase: 03-admin-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/school_class.py
  - app/models/__init__.py
  - app/models/user.py
  - app/models/schedule_entry.py
  - app/routers/admin.py
  - app/templates/admin_base.html
  - app/templates/base.html
  - app/static/admin.js
  - app/main.py
  - tests/test_admin.py
  - tests/conftest.py
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-10

must_haves:
  truths:
    - "Test stubs exist for all 10 ADMIN requirements"
    - "SchoolClass model creates its table on startup"
    - "Admin nav bar appears on admin pages with Geraete, Benutzer, Abmelden links"
    - "GET /admin redirects to /admin/devices"
  artifacts:
    - path: "app/models/school_class.py"
      provides: "SchoolClass ORM model"
      contains: "class SchoolClass"
    - path: "app/templates/admin_base.html"
      provides: "Admin base template with nav"
      contains: "admin_content"
    - path: "app/routers/admin.py"
      provides: "Admin router skeleton"
      contains: "router = APIRouter"
    - path: "tests/test_admin.py"
      provides: "10 test stubs for ADMIN requirements"
      min_lines: 80
    - path: "app/static/admin.js"
      provides: "Inline edit change detection JS"
      contains: "hasChanges"
  key_links:
    - from: "app/main.py"
      to: "app/routers/admin.py"
      via: "app.include_router(admin_router)"
      pattern: "include_router.*admin"
    - from: "app/templates/admin_base.html"
      to: "app/templates/base.html"
      via: "extends base.html"
      pattern: "extends.*base.html"
    - from: "app/models/__init__.py"
      to: "app/models/school_class.py"
      via: "import SchoolClass"
      pattern: "from app.models.school_class import SchoolClass"
---

<objective>
Create the foundation for the admin interface: SchoolClass model, admin router skeleton with redirect, admin base template with shared nav, vanilla JS for inline editing, and the full test scaffold (10 test stubs for all ADMIN requirements).

Purpose: Establish all shared infrastructure so Wave 2 plans can build device and user pages in parallel without conflicts.
Output: SchoolClass model, admin_base.html template, admin.py router skeleton, admin.js, test_admin.py with 10 stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-interface/03-CONTEXT.md
@.planning/phases/03-admin-interface/03-RESEARCH.md
@.planning/phases/03-admin-interface/03-UI-SPEC.md

<interfaces>
<!-- Existing models the executor needs -->
From app/models/device.py:
```python
class Device(Base):
    __tablename__ = "devices"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    device_id: Mapped[str] = mapped_column(String, unique=True, nullable=False, index=True)
    room: Mapped[str | None] = mapped_column(String, nullable=True)
    label: Mapped[str | None] = mapped_column(String, nullable=True)
    is_enabled: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    is_online: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    last_seen: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)
    last_lux: Mapped[float | None] = mapped_column(Float, nullable=True)
```

From app/models/user.py:
```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    email: Mapped[str] = mapped_column(String, unique=True, nullable=False, index=True)
    first_name: Mapped[str] = mapped_column(String, nullable=False)
    last_name: Mapped[str] = mapped_column(String, nullable=False)
    role: Mapped[str] = mapped_column(String, nullable=False)  # "admin" | "teacher" | "student"
    class_name: Mapped[str | None] = mapped_column(String, nullable=True)
    password_hash: Mapped[str] = mapped_column(String, nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, nullable=False)
```

From app/models/schedule_entry.py:
```python
class ScheduleEntry(Base):
    __tablename__ = "schedule_entries"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    device_id: Mapped[int] = mapped_column(Integer, ForeignKey("devices.id"), nullable=False)
    teacher_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    class_name: Mapped[str] = mapped_column(String, nullable=False)
    weekday: Mapped[int] = mapped_column(Integer, nullable=False)  # 0=Monday ... 6=Sunday
    start_time: Mapped[time] = mapped_column(Time, nullable=False)
    end_time: Mapped[time] = mapped_column(Time, nullable=False)
```

From app/dependencies.py:
```python
async def get_current_user(access_token: str | None = Cookie(default=None), db: Session = Depends(get_db)) -> User:
def require_role(*roles: str):  # returns dependency that checks user.role
```

From app/services/auth.py:
```python
def get_password_hash(password: str) -> str:
def verify_password(plain_password: str, hashed_password: str) -> bool:
```

From app/database.py:
```python
class Base(DeclarativeBase): pass
def get_db() -> Generator[Session, None, None]:
```

From app/main.py:
```python
app = FastAPI(title="SmartAttend", ...)
app.mount("/static", StaticFiles(directory="app/static"), name="static")
app.include_router(auth_router)
```

From tests/conftest.py:
```python
def make_user(db, email, password, role, is_active) -> User  # in test_auth.py, not conftest
# Fixtures: override_settings, db_session, test_client
```

From app/templates/base.html:
```html
<main class="container">
  {% block content %}{% endblock %}
</main>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchoolClass model + model registration + admin router skeleton + main.py wiring</name>
  <files>
    app/models/school_class.py,
    app/models/__init__.py,
    app/routers/admin.py,
    app/templates/admin_base.html,
    app/templates/base.html,
    app/static/admin.js,
    app/main.py
  </files>
  <read_first>
    app/models/device.py,
    app/models/user.py,
    app/models/schedule_entry.py,
    app/models/__init__.py,
    app/routers/auth.py,
    app/templates/base.html,
    app/templates/login.html,
    app/main.py,
    app/dependencies.py,
    app/database.py
  </read_first>
  <action>
**1. Create `app/models/school_class.py`** (per D-12):
```python
from sqlalchemy import Integer, String
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base


class SchoolClass(Base):
    __tablename__ = "school_classes"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    name: Mapped[str] = mapped_column(String, unique=True, nullable=False)
```

**2. Update `app/models/__init__.py`** — add SchoolClass import:
```python
from app.models.user import User
from app.models.device import Device
from app.models.schedule_entry import ScheduleEntry
from app.models.attendance_token import AttendanceToken
from app.models.attendance_record import AttendanceRecord
from app.models.school_class import SchoolClass

__all__ = ["User", "Device", "ScheduleEntry", "AttendanceToken", "AttendanceRecord", "SchoolClass"]
```

**3. Update `app/templates/base.html`** — add a `{% block head_extra %}{% endblock %}` inside `<head>` just before `</head>` so admin pages can include `admin.js`:
```html
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/static/pico.min.css">
  <title>{% block title %}SmartAttend{% endblock %}</title>
  {% block head_extra %}{% endblock %}
</head>
<body>
  <main class="container">
    {% block content %}{% endblock %}
  </main>
</body>
</html>
```

**4. Create `app/templates/admin_base.html`** (per D-02, D-03):
```html
{% extends "base.html" %}
{% block head_extra %}
<script src="/static/admin.js" defer></script>
{% endblock %}
{% block content %}
<nav>
  <ul>
    <li><strong>SmartAttend</strong></li>
  </ul>
  <ul>
    <li><a href="/admin/devices" {% if active_page == 'devices' %}aria-current="page"{% endif %}>Geräte</a></li>
    <li><a href="/admin/users" {% if active_page == 'users' %}aria-current="page"{% endif %}>Benutzer</a></li>
    <li>
      <form method="post" action="/auth/logout" style="margin: 0;">
        <button type="submit" class="outline secondary" style="margin: 0; padding: 0.5rem 1rem;">Abmelden</button>
      </form>
    </li>
  </ul>
</nav>
{% if request.query_params.get('msg') %}
<p role="alert" style="color: #2e7d32">{{ request.query_params.get('msg') }}</p>
{% endif %}
{% if request.query_params.get('error') %}
<p role="alert" style="color: var(--pico-del-color)">{{ request.query_params.get('error') }}</p>
{% endif %}
{% block admin_content %}{% endblock %}
{% endblock %}
```

**5. Create `app/routers/admin.py`** — skeleton with GET /admin redirect and placeholder pages:
```python
"""Admin interface router: device management, user management, schedule CRUD."""
from fastapi import APIRouter, Depends, Request
from fastapi.responses import RedirectResponse
from fastapi.templating import Jinja2Templates
from sqlalchemy.orm import Session

from app.database import get_db
from app.dependencies import require_role
from app.models.user import User

templates = Jinja2Templates(directory="app/templates")
router = APIRouter(prefix="/admin", tags=["admin"])


@router.get("")
async def admin_root(user: User = Depends(require_role("admin"))):
    """Redirect /admin to /admin/devices (primary admin page)."""
    return RedirectResponse(url="/admin/devices", status_code=303)


@router.get("/devices")
async def devices_page(
    request: Request,
    user: User = Depends(require_role("admin")),
    db: Session = Depends(get_db),
):
    """Device management page — placeholder, completed in Plan 02."""
    return templates.TemplateResponse(
        request=request,
        name="admin_devices.html",
        context={"user": user, "devices": [], "active_page": "devices"},
    )


@router.get("/users")
async def users_page(
    request: Request,
    user: User = Depends(require_role("admin")),
    db: Session = Depends(get_db),
):
    """User management page — placeholder, completed in Plan 03."""
    return templates.TemplateResponse(
        request=request,
        name="admin_users.html",
        context={"user": user, "users": [], "school_classes": [], "active_page": "users"},
    )
```

Create placeholder templates so the skeleton works:

**`app/templates/admin_devices.html`:**
```html
{% extends "admin_base.html" %}
{% block title %}Geräteverwaltung — SmartAttend{% endblock %}
{% block admin_content %}
<hgroup>
  <h1>Geräteverwaltung</h1>
  <p>Registrierte Geräte und deren Status</p>
</hgroup>
<p>Keine Geräte registriert.</p>
{% endblock %}
```

**`app/templates/admin_users.html`:**
```html
{% extends "admin_base.html" %}
{% block title %}Benutzerverwaltung — SmartAttend{% endblock %}
{% block admin_content %}
<hgroup>
  <h1>Benutzerverwaltung</h1>
  <p>Benutzer erstellen und verwalten</p>
</hgroup>
<p>Keine Benutzer vorhanden.</p>
{% endblock %}
```

**6. Create `app/static/admin.js`** — vanilla JS for inline edit change detection (per D-05, D-09):
```javascript
/**
 * Admin inline-edit change detection.
 * Tracks inputs/selects with data-original attribute.
 * Enables save/reset buttons only when changes exist.
 */
document.addEventListener('DOMContentLoaded', () => {
  // --- Inline edit change detection ---
  const form = document.getElementById('edit-form');
  if (form) {
    const inputs = form.querySelectorAll('input[data-original], select[data-original]');
    const saveBtn = document.getElementById('save-btn');
    const resetBtn = document.getElementById('reset-btn');

    function hasChanges() {
      return Array.from(inputs).some(el => el.value !== el.dataset.original);
    }

    function updateButtons() {
      const changed = hasChanges();
      if (saveBtn) saveBtn.disabled = !changed;
      if (resetBtn) resetBtn.disabled = !changed;
    }

    inputs.forEach(el => el.addEventListener('input', updateButtons));

    if (resetBtn) {
      resetBtn.addEventListener('click', () => {
        inputs.forEach(el => { el.value = el.dataset.original; });
        updateButtons();
      });
    }
  }

  // --- Schedule conflict check (used in Plan 04) ---
  const conflictForm = document.querySelector('[data-conflict-check]');
  if (conflictForm) {
    const addBtn = conflictForm.querySelector('[data-add-btn]');
    const errorDiv = conflictForm.querySelector('[data-conflict-error]');
    const fields = ['device_id', 'weekday', 'start_time', 'end_time'];

    async function checkConflict() {
      const params = new URLSearchParams();
      let allFilled = true;
      fields.forEach(f => {
        const el = conflictForm.querySelector(`[name="${f}"]`);
        if (el && el.value) {
          params.set(f, el.value);
        } else {
          allFilled = false;
        }
      });
      if (!allFilled) {
        if (addBtn) addBtn.disabled = true;
        if (errorDiv) errorDiv.textContent = '';
        return;
      }
      try {
        const resp = await fetch(`/admin/api/schedule/check-conflict?${params}`);
        const data = await resp.json();
        if (data.conflict) {
          if (errorDiv) errorDiv.textContent = data.message;
          if (addBtn) addBtn.disabled = true;
        } else {
          if (errorDiv) errorDiv.textContent = '';
          if (addBtn) addBtn.disabled = false;
        }
      } catch {
        if (addBtn) addBtn.disabled = false;
        if (errorDiv) errorDiv.textContent = '';
      }
    }

    fields.forEach(f => {
      const el = conflictForm.querySelector(`[name="${f}"]`);
      if (el) el.addEventListener('change', checkConflict);
    });
  }
});
```

**7. Update `app/main.py`** — register admin router after auth_router:
Add these two lines at the bottom of the file, after the existing `app.include_router(auth_router)`:
```python
from app.routers.admin import router as admin_router  # noqa: E402
app.include_router(admin_router)
```
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && python -c "from app.models.school_class import SchoolClass; print('SchoolClass OK')" && python -c "from app.routers.admin import router; print('Admin router OK')" && pytest tests/test_auth.py -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/models/school_class.py contains `class SchoolClass(Base):`
    - app/models/school_class.py contains `__tablename__ = "school_classes"`
    - app/models/school_class.py contains `name: Mapped[str] = mapped_column(String, unique=True, nullable=False)`
    - app/models/__init__.py contains `from app.models.school_class import SchoolClass`
    - app/models/__init__.py contains `"SchoolClass"` in __all__
    - app/templates/base.html contains `{% block head_extra %}{% endblock %}`
    - app/templates/admin_base.html contains `{% extends "base.html" %}`
    - app/templates/admin_base.html contains `href="/admin/devices"`
    - app/templates/admin_base.html contains `href="/admin/users"`
    - app/templates/admin_base.html contains `action="/auth/logout"`
    - app/templates/admin_base.html contains `{% block admin_content %}{% endblock %}`
    - app/templates/admin_base.html contains `aria-current="page"`
    - app/routers/admin.py contains `router = APIRouter(prefix="/admin"`
    - app/routers/admin.py contains `require_role("admin")`
    - app/routers/admin.py contains `RedirectResponse(url="/admin/devices", status_code=303)`
    - app/static/admin.js contains `function hasChanges()`
    - app/static/admin.js contains `data-conflict-check`
    - app/main.py contains `from app.routers.admin import router as admin_router`
    - app/main.py contains `app.include_router(admin_router)`
    - app/templates/admin_devices.html contains `{% extends "admin_base.html" %}`
    - app/templates/admin_users.html contains `{% extends "admin_base.html" %}`
    - Existing tests pass: `pytest tests/test_auth.py -x` exits 0
  </acceptance_criteria>
  <done>SchoolClass model exists, admin router skeleton serves /admin (redirect), /admin/devices, /admin/users with nav bar. admin.js provides inline-edit and conflict-check JS. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Test scaffold — 10 test stubs + admin auth helper fixture</name>
  <files>
    tests/test_admin.py,
    tests/conftest.py
  </files>
  <read_first>
    tests/conftest.py,
    tests/test_auth.py,
    app/models/device.py,
    app/models/user.py,
    app/models/schedule_entry.py,
    app/models/school_class.py,
    app/routers/admin.py
  </read_first>
  <action>
**1. Add `admin_client` fixture to `tests/conftest.py`** — creates an admin user, logs in, returns a test_client with the auth cookie set:
```python
@pytest.fixture
def admin_client(test_client, db_session):
    """TestClient pre-authenticated as admin. Creates admin user and logs in."""
    from app.services.auth import get_password_hash
    from app.models.user import User

    admin = User(
        email="admin@test.com",
        first_name="Admin",
        last_name="Test",
        role="admin",
        password_hash=get_password_hash("adminpass"),
        is_active=True,
    )
    db_session.add(admin)
    db_session.commit()

    resp = test_client.post(
        "/auth/login",
        data={"email": "admin@test.com", "password": "adminpass", "next": ""},
    )
    assert resp.status_code == 303
    # Cookie is now set on test_client
    return test_client
```

Also add helper fixtures for seeding test data:
```python
@pytest.fixture
def seed_device(db_session):
    """Create a test device. Returns the Device object."""
    from app.models.device import Device

    device = Device(
        device_id="test-device-001",
        room="R101",
        label="Raum 101",
        is_enabled=False,
        is_online=True,
    )
    db_session.add(device)
    db_session.commit()
    db_session.refresh(device)
    return device


@pytest.fixture
def seed_teacher(db_session):
    """Create a test teacher. Returns the User object."""
    from app.services.auth import get_password_hash
    from app.models.user import User

    teacher = User(
        email="teacher@test.com",
        first_name="Lehrer",
        last_name="Test",
        role="teacher",
        password_hash=get_password_hash("teacherpass"),
        is_active=True,
    )
    db_session.add(teacher)
    db_session.commit()
    db_session.refresh(teacher)
    return teacher


@pytest.fixture
def seed_school_class(db_session):
    """Create a test SchoolClass. Returns the SchoolClass object."""
    from app.models.school_class import SchoolClass

    sc = SchoolClass(name="10A")
    db_session.add(sc)
    db_session.commit()
    db_session.refresh(sc)
    return sc
```

**2. Create `tests/test_admin.py`** with 10 test stubs (one per ADMIN requirement), each with a descriptive docstring and `pytest.skip("Plan XX implements this")`:

```python
"""
Phase 3 Admin Interface tests.
Maps to ADMIN-01 through ADMIN-10.
"""
import pytest
from datetime import time


# ── ADMIN-01: View devices with status ────────────────────────────
def test_devices_page_shows_devices(admin_client, db_session, seed_device):
    """GET /admin/devices shows device table with device_id, room, status (ADMIN-01)."""
    pytest.skip("Plan 02 implements this")


# ── ADMIN-02: Assign room and label ──────────────────────────────
def test_update_device_room_label(admin_client, db_session, seed_device):
    """POST /admin/devices/update changes room and label fields (ADMIN-02)."""
    pytest.skip("Plan 02 implements this")


# ── ADMIN-03: Enable/disable device ──────────────────────────────
def test_toggle_device_enabled(admin_client, db_session, seed_device):
    """POST /admin/devices/{id}/toggle flips is_enabled (ADMIN-03)."""
    pytest.skip("Plan 02 implements this")


# ── ADMIN-04: View all users ─────────────────────────────────────
def test_users_page_shows_users(admin_client, db_session):
    """GET /admin/users shows user table with name, role, class (ADMIN-04)."""
    pytest.skip("Plan 03 implements this")


# ── ADMIN-05: Create new user ────────────────────────────────────
def test_create_user(admin_client, db_session, seed_school_class):
    """POST /admin/users/create creates a new user with hashed password (ADMIN-05)."""
    pytest.skip("Plan 03 implements this")


# ── ADMIN-06: Deactivate user ────────────────────────────────────
def test_deactivate_user(admin_client, db_session):
    """POST /admin/users/{id}/deactivate sets is_active=False (ADMIN-06)."""
    pytest.skip("Plan 03 implements this")


# ── ADMIN-07: View schedule entries per device ───────────────────
def test_device_schedule_entries_shown(admin_client, db_session, seed_device, seed_teacher, seed_school_class):
    """GET /admin/devices shows schedule entries within device details (ADMIN-07)."""
    pytest.skip("Plan 04 implements this")


# ── ADMIN-08: Add schedule entry ─────────────────────────────────
def test_add_schedule_entry(admin_client, db_session, seed_device, seed_teacher, seed_school_class):
    """POST /admin/schedule/add creates a ScheduleEntry (ADMIN-08)."""
    pytest.skip("Plan 04 implements this")


# ── ADMIN-09: Reject conflicting schedule ─────────────────────────
def test_schedule_conflict_rejected(admin_client, db_session, seed_device, seed_teacher, seed_school_class):
    """POST /admin/schedule/add with overlapping time is rejected (ADMIN-09)."""
    pytest.skip("Plan 04 implements this")


# ── ADMIN-10: Delete schedule entry ──────────────────────────────
def test_delete_schedule_entry(admin_client, db_session, seed_device, seed_teacher, seed_school_class):
    """POST /admin/schedule/{id}/delete removes the entry (ADMIN-10)."""
    pytest.skip("Plan 04 implements this")


# ── Skeleton tests (verify Plan 01 infrastructure) ───────────────
def test_admin_redirect(admin_client):
    """GET /admin redirects to /admin/devices with 303."""
    resp = admin_client.get("/admin")
    assert resp.status_code == 303
    assert "/admin/devices" in resp.headers["location"]


def test_admin_devices_page_renders(admin_client):
    """GET /admin/devices returns 200 with nav bar."""
    resp = admin_client.get("/admin/devices", follow_redirects=True)
    assert resp.status_code == 200
    assert "Geräte" in resp.text or "Ger" in resp.text
    assert "Benutzer" in resp.text


def test_admin_users_page_renders(admin_client):
    """GET /admin/users returns 200 with nav bar."""
    resp = admin_client.get("/admin/users", follow_redirects=True)
    assert resp.status_code == 200
    assert "Benutzerverwaltung" in resp.text


def test_admin_requires_auth(test_client):
    """GET /admin/devices without auth redirects to /login."""
    resp = test_client.get("/admin/devices")
    assert resp.status_code == 303
    assert "/login" in resp.headers["location"]


def test_admin_requires_admin_role(test_client, db_session):
    """GET /admin/devices with teacher role returns 403."""
    from app.services.auth import get_password_hash
    from app.models.user import User

    teacher = User(
        email="teacher-nonadmin@test.com",
        first_name="T",
        last_name="T",
        role="teacher",
        password_hash=get_password_hash("pass"),
        is_active=True,
    )
    db_session.add(teacher)
    db_session.commit()

    resp = test_client.post(
        "/auth/login",
        data={"email": "teacher-nonadmin@test.com", "password": "pass", "next": ""},
    )
    assert resp.status_code == 303
    resp = test_client.get("/admin/devices")
    assert resp.status_code == 403
```
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && pytest tests/test_admin.py -x -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_admin.py contains `def test_devices_page_shows_devices`
    - tests/test_admin.py contains `def test_update_device_room_label`
    - tests/test_admin.py contains `def test_toggle_device_enabled`
    - tests/test_admin.py contains `def test_users_page_shows_users`
    - tests/test_admin.py contains `def test_create_user`
    - tests/test_admin.py contains `def test_deactivate_user`
    - tests/test_admin.py contains `def test_device_schedule_entries_shown`
    - tests/test_admin.py contains `def test_add_schedule_entry`
    - tests/test_admin.py contains `def test_schedule_conflict_rejected`
    - tests/test_admin.py contains `def test_delete_schedule_entry`
    - tests/test_admin.py contains `def test_admin_redirect`
    - tests/test_admin.py contains `def test_admin_requires_auth`
    - tests/conftest.py contains `def admin_client`
    - tests/conftest.py contains `def seed_device`
    - tests/conftest.py contains `def seed_teacher`
    - tests/conftest.py contains `def seed_school_class`
    - `pytest tests/test_admin.py -x` exits 0 (10 skipped + 5 passing infrastructure tests)
    - `pytest tests/ -x` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <done>10 ADMIN requirement test stubs exist (skipped), 5 infrastructure tests pass (redirect, pages render, auth required, role check). admin_client, seed_device, seed_teacher, seed_school_class fixtures available in conftest.py. Full test suite green.</done>
</task>

</tasks>

<verification>
- `python -c "from app.models.school_class import SchoolClass; print(SchoolClass.__tablename__)"` prints `school_classes`
- `pytest tests/test_admin.py -v` shows 10 skipped + 5 passed
- `pytest tests/ -x` exits 0 (full suite green)
- GET /admin returns 303 redirect to /admin/devices
- GET /admin/devices (authenticated) returns 200 with nav containing Geraete, Benutzer, Abmelden
</verification>

<success_criteria>
- SchoolClass model registered and creates table
- Admin router serves /admin (redirect), /admin/devices, /admin/users
- Admin base template has shared nav with 3 links and flash message support
- admin.js provides inline-edit change detection and conflict-check logic
- 10 test stubs exist, 5 infrastructure tests pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-interface/03-01-SUMMARY.md`
</output>
