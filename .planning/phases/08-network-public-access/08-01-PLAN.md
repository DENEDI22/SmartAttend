---
phase: 08-network-public-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/scheduler.py
  - .env.example
  - .env
  - tests/conftest.py
  - tests/test_foundation.py
  - tests/test_scheduler.py
autonomous: true
requirements: [NET-01, NET-03, NET-02]

must_haves:
  truths:
    - "Token URLs use BASE_URL instead of SERVER_IP"
    - "BASE_URL includes scheme (https://...) so no hardcoded http://"
    - "MQTT port 1883 is exposed on host for LAN ESP32 access"
    - "Ngrok env vars (NGROK_AUTHTOKEN, NGROK_DOMAIN) documented in .env.example"
    - "All existing tests pass with updated config"
  artifacts:
    - path: "app/config.py"
      provides: "Settings class with base_url field"
      contains: "base_url"
    - path: "app/services/scheduler.py"
      provides: "Token URL generation using base_url"
      contains: "settings.base_url"
    - path: ".env.example"
      provides: "Reference for all env vars including BASE_URL and ngrok"
      contains: "BASE_URL"
    - path: "tests/conftest.py"
      provides: "Test env override with BASE_URL"
      contains: "BASE_URL"
  key_links:
    - from: "app/services/scheduler.py"
      to: "app/config.py"
      via: "settings.base_url"
      pattern: "settings\\.base_url"
    - from: "tests/conftest.py"
      to: "app/config.py"
      via: "monkeypatch BASE_URL"
      pattern: "BASE_URL"
---

<objective>
Migrate server configuration from SERVER_IP to BASE_URL and add ngrok environment variables.

Purpose: Token URLs sent to ESP32 devices must use the ngrok public domain (or any configurable base URL) instead of a raw IP. This is the config foundation for public internet access.
Output: Updated config, scheduler, env files, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-network-public-access/08-CONTEXT.md

<interfaces>
<!-- Current Settings class in app/config.py -->
```python
class Settings(BaseSettings):
    secret_key: str
    database_url: str = "sqlite:////app/data/smartattend.db"
    mqtt_broker: str = "mqtt"
    mqtt_port: int = 1883
    server_ip: str = "localhost"  # TO BE REPLACED with base_url
    admin_email: str = ""
    admin_password: str = ""
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")
```

<!-- Current token URL generation in app/services/scheduler.py line 77 -->
```python
url = f"http://{settings.server_ip}/checkin?token={token_str}"
```

<!-- Current test env override in tests/conftest.py line 31 -->
```python
monkeypatch.setenv("SERVER_IP", "127.0.0.1")
```

<!-- Current test assertion in tests/test_scheduler.py line 223 -->
```python
assert url_arg.startswith("http://127.0.0.1/checkin?token=")
```

<!-- Current test in tests/test_foundation.py line 85 -->
```python
required_vars = ["SECRET_KEY", "DATABASE_URL", "MQTT_BROKER", "MQTT_PORT", "SERVER_IP"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SERVER_IP with BASE_URL in config, scheduler, and env files</name>
  <files>app/config.py, app/services/scheduler.py, .env.example, .env</files>
  <read_first>app/config.py, app/services/scheduler.py, .env.example, .env</read_first>
  <action>
Per D-01, D-02, D-03, D-04, D-05:

1. **app/config.py** — In the Settings class:
   - Remove the line `server_ip: str = "localhost"`
   - Add `base_url: str = "http://localhost:8000"` in the `# Network` section
   - Keep all other fields unchanged

2. **app/services/scheduler.py** — Line 77:
   - Change `url = f"http://{settings.server_ip}/checkin?token={token_str}"`
   - To: `url = f"{settings.base_url}/checkin?token={token_str}"`
   - Note: `base_url` already includes the scheme, so no `http://` prefix

3. **.env.example** — In the `# ── Network` section:
   - Remove `SERVER_IP=localhost` and its comment
   - Replace with:
     ```
     # Public base URL used in check-in token URLs sent to NFC devices
     # For development: http://localhost:8000
     # For production with ngrok: https://your-domain.ngrok-free.app
     BASE_URL=http://localhost:8000
     ```
   - Add a new section after Network:
     ```
     # ── Ngrok (production only) ────────────────────────────────────────────
     # Get auth token from https://dashboard.ngrok.com
     # Reserve a domain at https://dashboard.ngrok.com/domains
     NGROK_AUTHTOKEN=
     NGROK_DOMAIN=
     ```

4. **.env** — Replace `SERVER_IP=localhost` with `BASE_URL=http://localhost:8000`. Add `NGROK_AUTHTOKEN=` and `NGROK_DOMAIN=` at the end.
  </action>
  <verify>
    <automated>grep -q "base_url" app/config.py && grep -q "settings.base_url" app/services/scheduler.py && grep -q "BASE_URL" .env.example && grep -q "BASE_URL" .env && grep -q "NGROK_AUTHTOKEN" .env.example && ! grep -q "server_ip" app/config.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/config.py contains `base_url: str = "http://localhost:8000"` and does NOT contain `server_ip`
    - app/services/scheduler.py line 77 area contains `f"{settings.base_url}/checkin?token={token_str}"` and does NOT contain `settings.server_ip`
    - .env.example contains `BASE_URL=http://localhost:8000` and does NOT contain `SERVER_IP=`
    - .env.example contains `NGROK_AUTHTOKEN=` and `NGROK_DOMAIN=`
    - .env contains `BASE_URL=http://localhost:8000` and does NOT contain `SERVER_IP=`
  </acceptance_criteria>
  <done>SERVER_IP fully replaced by BASE_URL in all config and env files. Ngrok env vars documented.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests for BASE_URL configuration</name>
  <files>tests/conftest.py, tests/test_foundation.py, tests/test_scheduler.py</files>
  <read_first>tests/conftest.py, tests/test_foundation.py, tests/test_scheduler.py</read_first>
  <action>
1. **tests/conftest.py** — In the `override_settings` fixture:
   - Remove `monkeypatch.setenv("SERVER_IP", "127.0.0.1")`
   - Add `monkeypatch.setenv("BASE_URL", "http://127.0.0.1:8000")`
   - Keep all other monkeypatch lines unchanged

2. **tests/test_foundation.py** — In `test_env_example_complete` around line 85:
   - Change the `required_vars` list from:
     `["SECRET_KEY", "DATABASE_URL", "MQTT_BROKER", "MQTT_PORT", "SERVER_IP"]`
   - To:
     `["SECRET_KEY", "DATABASE_URL", "MQTT_BROKER", "MQTT_PORT", "BASE_URL"]`

3. **tests/test_scheduler.py** — In `test_token_url_format` around line 223:
   - Change `assert url_arg.startswith("http://127.0.0.1/checkin?token=")`
   - To: `assert url_arg.startswith("http://127.0.0.1:8000/checkin?token=")`
   - The URL now comes from BASE_URL which includes the port
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && python -m pytest tests/test_foundation.py tests/test_scheduler.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `monkeypatch.setenv("BASE_URL", "http://127.0.0.1:8000")` and does NOT contain `SERVER_IP`
    - tests/test_foundation.py required_vars list contains `"BASE_URL"` and does NOT contain `"SERVER_IP"`
    - tests/test_scheduler.py contains `url_arg.startswith("http://127.0.0.1:8000/checkin?token=")`
    - `python -m pytest tests/ -x -q` exits with code 0
  </acceptance_criteria>
  <done>All tests updated to use BASE_URL and pass successfully.</done>
</task>

</tasks>

<verification>
1. `grep -r "server_ip\|SERVER_IP" app/ .env .env.example tests/` returns NO matches
2. `grep -r "base_url\|BASE_URL" app/ .env .env.example tests/` returns matches in all expected files
3. `python -m pytest tests/ -x -q` passes all tests
</verification>

<success_criteria>
- SERVER_IP completely removed from codebase (config, env, tests)
- BASE_URL used everywhere with scheme included
- Ngrok env vars (NGROK_AUTHTOKEN, NGROK_DOMAIN) in .env.example
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-network-public-access/08-01-SUMMARY.md`
</output>
