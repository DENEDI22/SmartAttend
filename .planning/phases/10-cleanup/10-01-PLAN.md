---
phase: 10-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/mqtt.py
  - app/models/device.py
  - app/templates/admin_devices.html
  - dummy_client/main.py
  - tests/test_mqtt.py
  - tests/test_dummy_client.py
  - app/routers/auth.py
  - app/services/scheduler.py
  - tests/test_scheduler.py
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "No lux-related code exists in MQTT handlers, models, dummy client, or templates"
    - "Check-in tokens rotate every 90 seconds instead of 60 seconds"
    - "Student JWT session lasts 30 days so they stay logged in across browser restarts"
  artifacts:
    - path: "app/services/mqtt.py"
      provides: "MQTT handler without lux subscription or handler"
      contains: "devices/register"
    - path: "app/models/device.py"
      provides: "Device model without last_lux column"
    - path: "app/templates/admin_devices.html"
      provides: "Device table without Lux column"
    - path: "dummy_client/main.py"
      provides: "Dummy client without lux loop or LUX_VALUE"
    - path: "app/services/scheduler.py"
      provides: "Token issuance running every 90 seconds"
      contains: "IntervalTrigger(seconds=90)"
    - path: "app/routers/auth.py"
      provides: "Student JWT with 30-day expiry"
      contains: "timedelta(days=30)"
  key_links:
    - from: "app/routers/auth.py"
      to: "app/services/auth.py"
      via: "create_access_token with 30-day timedelta for students"
      pattern: "timedelta\\(days=30\\)"
    - from: "app/services/scheduler.py"
      to: "apscheduler"
      via: "IntervalTrigger with 90-second interval"
      pattern: "seconds=90"
---

<objective>
Remove dead lux-sensor code from the codebase and tune token/session lifetimes for better UX.

Purpose: Clean up unused lux feature that was never needed, extend check-in token rotation to 90s to reduce unnecessary MQTT churn, and extend student sessions to 30 days so students do not have to log in repeatedly.

Output: Cleaner codebase with no lux references, 90-second token rotation, and 30-day student sessions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/services/mqtt.py
@app/models/device.py
@app/templates/admin_devices.html
@dummy_client/main.py
@tests/test_mqtt.py
@tests/test_dummy_client.py
@app/routers/auth.py
@app/services/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all lux-related code (CLN-01)</name>
  <files>app/services/mqtt.py, app/models/device.py, app/templates/admin_devices.html, dummy_client/main.py, tests/test_mqtt.py, tests/test_dummy_client.py</files>
  <read_first>app/services/mqtt.py, app/models/device.py, app/templates/admin_devices.html, dummy_client/main.py, tests/test_mqtt.py, tests/test_dummy_client.py</read_first>
  <action>
Remove all lux-related code per CLN-01. Specific changes:

1. **app/services/mqtt.py**:
   - Remove `("sensors/+/lux", 0)` from the subscription list in `_on_connect` (line 23). Keep `("devices/register", 0)` and `("devices/+/status", 0)`.
   - Remove the `elif topic.startswith("sensors/") and topic.endswith("/lux"):` branch in `_on_message` (lines 43-47).
   - Delete the entire `_handle_lux` function (lines 95-105).
   - Update the module docstring from "handles registration/heartbeat/lux" to "handles registration/heartbeat".

2. **app/models/device.py**:
   - Remove the line `last_lux: Mapped[float | None] = mapped_column(Float, nullable=True)` (line 17).
   - Remove `Float` from the sqlalchemy imports if it is no longer used (check if any other column uses Float -- it is only used by last_lux, so remove it).

3. **app/templates/admin_devices.html**:
   - Remove the `<th>Lux</th>` header cell (line 21).
   - Remove the `<td>{{ '%.1f' % device.last_lux ... }}</td>` data cell (line 52).

4. **dummy_client/main.py**:
   - Remove the `LUX_VALUE = float(os.environ.get("LUX_VALUE", "400"))` line (line 22).
   - Delete the entire `lux_loop` function (lines 72-78).
   - Remove the `threading.Thread(target=lux_loop, ...)` line in `main()` (line 106).
   - Update the module docstring from "heartbeat every 30s, lux every 60s" to "heartbeat every 30s".
   - Update the log line in `main()` from `"Starting dummy client: device=%s room=%s lux=%s", DEVICE_ID, ROOM, LUX_VALUE` to `"Starting dummy client: device=%s room=%s", DEVICE_ID, ROOM`.

5. **tests/test_mqtt.py**:
   - Remove `("sensors/+/lux", 0)` from the expected subscription list in `test_on_connect_subscribes` (line 50).
   - Delete the entire `test_lux_update` test function (lines 106-117).

6. **tests/test_dummy_client.py**:
   - Remove `"LUX_VALUE": "400"` from the default env dict used to reload the module (line 22).
   - Remove the `LUX_VALUE="123.5"` test for env overrides and its assertion `assert mod.LUX_VALUE == 123.5` (lines 46, 52).
   - Delete the entire `TestLux` class (lines 126-156).
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && grep -ri "lux" app/ dummy_client/ tests/ --include="*.py" --include="*.html" | grep -v __pycache__ | grep -v ".pyc"; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "lux" app/ dummy_client/ tests/ --include="*.py" --include="*.html"` returns zero matches (excluding __pycache__)
    - `app/models/device.py` does NOT contain `last_lux` or `Float`
    - `app/services/mqtt.py` does NOT contain `_handle_lux` or `sensors/+/lux`
    - `dummy_client/main.py` does NOT contain `lux_loop` or `LUX_VALUE`
    - `app/templates/admin_devices.html` does NOT contain `Lux` or `last_lux`
    - `pytest tests/test_mqtt.py tests/test_dummy_client.py -x` passes
  </acceptance_criteria>
  <done>Zero lux references remain in application code, models, templates, dummy client, and tests. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Tune token rotation to 90s and student session to 30 days (CLN-02, CLN-03)</name>
  <files>app/services/scheduler.py, app/routers/auth.py, tests/test_scheduler.py</files>
  <read_first>app/services/scheduler.py, app/routers/auth.py, tests/test_scheduler.py</read_first>
  <action>
Two config changes per CLN-02 and CLN-03:

1. **app/services/scheduler.py** (CLN-02 -- token rotation from 60s to 90s):
   - Change `IntervalTrigger(minutes=1)` on line 114 to `IntervalTrigger(seconds=90)`.
   - Update the log message on line 125 from `"token issuance (1min)"` to `"token issuance (90s)"`.

2. **app/routers/auth.py** (CLN-03 -- student session from 1h to 30 days):
   - Change line 63 from `expires = timedelta(hours=1)` to `expires = timedelta(days=30)`.
   - The `max_age=int(expires.total_seconds())` on line 91 automatically picks up the new value (30 days = 2592000 seconds), so the JWT expiry and cookie max_age stay in lockstep as required by the STATE.md blocker note.
   - Teacher/admin expiry (`timedelta(hours=8)`) remains unchanged.

3. **tests/test_scheduler.py** (verify interval expectation if tested):
   - Check if any test asserts the interval value. If a test checks for `minutes=1`, update it to check for `seconds=90`. If no such assertion exists, no change needed.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && grep -n "seconds=90" app/services/scheduler.py && grep -n "days=30" app/routers/auth.py && pytest tests/ -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/scheduler.py` contains `IntervalTrigger(seconds=90)` (not `minutes=1`)
    - `app/services/scheduler.py` log message contains `"90s"` (not `"1min"`)
    - `app/routers/auth.py` contains `expires = timedelta(days=30)` for student role
    - `app/routers/auth.py` still contains `expires = timedelta(hours=8)` for non-student roles
    - `pytest tests/ -x` passes
  </acceptance_criteria>
  <done>Token rotation runs every 90 seconds. Student JWT and cookie expire after 30 days. Teacher/admin sessions unchanged at 8 hours. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -ri "lux" app/ dummy_client/ tests/ --include="*.py" --include="*.html"` returns no matches
2. `grep "seconds=90" app/services/scheduler.py` matches
3. `grep "days=30" app/routers/auth.py` matches
4. `pytest tests/ -x` -- full test suite passes
</verification>

<success_criteria>
- No lux-related code exists anywhere in the application (CLN-01)
- Token rotation interval is 90 seconds (CLN-02)
- Student JWT expires after 30 days, cookie max_age matches (CLN-03)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-cleanup/10-01-SUMMARY.md`
</output>
