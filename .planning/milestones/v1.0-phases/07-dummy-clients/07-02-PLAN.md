---
phase: 07-dummy-clients
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - dummy_client/Dockerfile
  - dummy_client/requirements.txt
autonomous: false
requirements:
  - DUMMY-06
  - DUMMY-07

must_haves:
  truths:
    - "dummy_client/Dockerfile builds a working image with paho-mqtt installed"
    - "dummy_client/requirements.txt pins paho-mqtt==2.1.0"
    - "Three containers (client-e101, client-e102, client-e103) start and connect to the broker"
  artifacts:
    - path: "dummy_client/Dockerfile"
      provides: "Docker image for dummy MQTT client"
      contains: "requirements.txt"
    - path: "dummy_client/requirements.txt"
      provides: "Python dependencies for dummy client"
      contains: "paho-mqtt==2.1.0"
  key_links:
    - from: "dummy_client/Dockerfile"
      to: "docker-compose.yml"
      via: "build context ./dummy_client"
      pattern: "build.*dummy_client"
    - from: "dummy_client/requirements.txt"
      to: "dummy_client/Dockerfile"
      via: "COPY and pip install"
      pattern: "pip install.*requirements"
---

<objective>
Update the dummy client Dockerfile to install paho-mqtt, add a requirements.txt, and verify all three containers boot correctly via Docker Compose.

Purpose: Completes the Docker infrastructure so the full SmartAttend stack runs end-to-end with three simulated ESP32 devices.
Output: Updated Dockerfile, new requirements.txt, and verified container startup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dummy-clients/07-CONTEXT.md
@.planning/phases/07-dummy-clients/07-RESEARCH.md
@.planning/phases/07-dummy-clients/07-01-SUMMARY.md

@dummy_client/Dockerfile
@dummy_client/main.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfile and add requirements.txt</name>
  <files>dummy_client/Dockerfile, dummy_client/requirements.txt</files>
  <read_first>
    - dummy_client/Dockerfile
    - dummy_client/main.py
    - docker-compose.yml
  </read_first>
  <action>
**Create `dummy_client/requirements.txt`:**
```
paho-mqtt==2.1.0
```

**Rewrite `dummy_client/Dockerfile`:**
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
ENV PYTHONUNBUFFERED=1
CMD ["python", "main.py"]
```

Key changes from Phase 1 Dockerfile:
- Add `COPY requirements.txt .` and `RUN pip install --no-cache-dir -r requirements.txt` before copying main.py (layer caching optimization)
- Add `ENV PYTHONUNBUFFERED=1` so print/logging output appears immediately in `docker compose logs` (Pitfall 5 from research)
- Keep `python:3.11-slim` base image (already multi-arch: linux/amd64, linux/arm64, linux/arm/v7)

No changes to `docker-compose.yml` needed -- it already defines all three client containers with correct env vars and `build: ./dummy_client`.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && cat dummy_client/requirements.txt && echo "---" && head -10 dummy_client/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - grep "paho-mqtt==2.1.0" dummy_client/requirements.txt succeeds
    - grep "COPY requirements.txt" dummy_client/Dockerfile succeeds
    - grep "pip install --no-cache-dir -r requirements.txt" dummy_client/Dockerfile succeeds
    - grep "PYTHONUNBUFFERED=1" dummy_client/Dockerfile succeeds
    - grep "COPY main.py" dummy_client/Dockerfile succeeds
    - grep "python:3.11-slim" dummy_client/Dockerfile succeeds
  </acceptance_criteria>
  <done>dummy_client/Dockerfile installs paho-mqtt from requirements.txt, sets PYTHONUNBUFFERED=1, and uses python:3.11-slim base image. requirements.txt pins paho-mqtt==2.1.0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Docker Compose startup</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml
    - dummy_client/Dockerfile
    - dummy_client/main.py
  </read_first>
  <action>
Run `docker compose build client-e101 client-e102 client-e103` then `docker compose up -d mqtt client-e101 client-e102 client-e103`. Check `docker compose logs --tail=20 client-e101 client-e102 client-e103` for connection and registration messages. Human verifies output.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && docker compose config --services | sort</automated>
  </verify>
  <what-built>Complete dummy client system: three Python MQTT containers (client-e101, client-e102, client-e103) that simulate ESP32 devices. Each publishes registration, heartbeat (30s), and lux readings (60s) to the Mosquitto broker, and subscribes to receive token URLs.</what-built>
  <how-to-verify>
    1. Build and start the dummy clients:
       ```
       docker compose build client-e101 client-e102 client-e103
       docker compose up -d mqtt client-e101 client-e102 client-e103
       ```
    2. Wait ~10 seconds for containers to connect.
    3. Check logs for all three clients:
       ```
       docker compose logs --tail=20 client-e101 client-e102 client-e103
       ```
    4. Expected in each client's logs:
       - "Starting dummy client {id} (room={room}, lux={lux})"
       - "Connected to broker mqtt:1883"
       - "Published registration"
       - "Subscribed to attendance/device/{id}"
    5. After ~30s, heartbeat messages should appear at DEBUG level (may need to wait or check broker).
    6. Stop: `docker compose down`
  </how-to-verify>
  <resume-signal>Type "approved" if all three clients connect and register, or describe issues.</resume-signal>
  <acceptance_criteria>
    - docker compose build succeeds for all three client containers
    - docker compose up starts all three without crash loops
    - docker compose logs shows connection and registration messages for e101, e102, e103
  </acceptance_criteria>
  <done>All three dummy client containers build, start, connect to the MQTT broker, publish registration, and appear healthy in docker compose logs.</done>
</task>

</tasks>

<verification>
1. `dummy_client/requirements.txt` contains `paho-mqtt==2.1.0`
2. `dummy_client/Dockerfile` installs from requirements.txt and sets PYTHONUNBUFFERED=1
3. `docker compose build client-e101` succeeds (builds the image)
4. `docker compose up -d mqtt client-e101 client-e102 client-e103` starts all containers
5. Container logs show successful MQTT connection and registration for all 3 clients
</verification>

<success_criteria>
- dummy_client/Dockerfile builds a working image with paho-mqtt
- dummy_client/requirements.txt pins paho-mqtt==2.1.0
- Three containers start from docker compose up and connect to the broker
- No crash loops or import errors in container logs
- Registration messages visible in logs for all three devices
</success_criteria>

<output>
After completion, create `.planning/phases/07-dummy-clients/07-02-SUMMARY.md`
</output>
