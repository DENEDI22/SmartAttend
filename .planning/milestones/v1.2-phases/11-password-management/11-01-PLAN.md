---
phase: 11-password-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/auth.py
  - app/templates/_password_change.html
  - app/templates/student.html
  - app/templates/teacher_dashboard.html
  - app/templates/admin_base.html
  - tests/test_password.py
autonomous: true
requirements: [PWD-01]

must_haves:
  truths:
    - "User can change their password by entering current password, new password, and confirmation"
    - "User sees an error if current password is wrong"
    - "User sees an error if new passwords do not match"
    - "After password change, user can log in with the new password"
  artifacts:
    - path: "app/routers/auth.py"
      provides: "POST /auth/password endpoint"
      contains: "change_password"
    - path: "app/templates/_password_change.html"
      provides: "Reusable password change form partial"
      contains: "current_password"
    - path: "tests/test_password.py"
      provides: "Password change tests"
      contains: "test_change_password"
  key_links:
    - from: "app/templates/_password_change.html"
      to: "/auth/password"
      via: "form POST action"
      pattern: "action=./auth/password."
    - from: "app/routers/auth.py"
      to: "app/services/auth.py"
      via: "verify_password and get_password_hash"
      pattern: "verify_password|get_password_hash"
---

<objective>
Self-service password change for all user roles.

Purpose: Users (students, teachers, admins) can change their own password from their dashboard without admin intervention (PWD-01).
Output: POST endpoint, reusable template partial, form embedded in all three role dashboards, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-password-management/11-CONTEXT.md

@app/services/auth.py
@app/routers/auth.py
@app/dependencies.py
@app/templates/student.html
@app/templates/teacher_dashboard.html
@app/templates/admin_base.html

<interfaces>
<!-- Key functions the executor needs from auth.py -->
From app/services/auth.py:
```python
def verify_password(plain_password: str, hashed_password: str) -> bool:
def get_password_hash(password: str) -> str:
```

From app/dependencies.py:
```python
async def get_current_user(...) -> User:
def require_role(*roles: str):
```

From app/routers/auth.py (existing patterns):
- Uses `Form(...)` for POST parameters
- Uses `templates.TemplateResponse` for rendering
- Uses `RedirectResponse(url=..., status_code=303)` for redirects
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /auth/password endpoint and password change form partial</name>
  <files>app/routers/auth.py, app/templates/_password_change.html, app/templates/student.html, app/templates/teacher_dashboard.html, app/templates/admin_base.html, tests/test_password.py</files>
  <read_first>app/routers/auth.py, app/services/auth.py, app/dependencies.py, app/templates/student.html, app/templates/teacher_dashboard.html, app/templates/admin_base.html, tests/conftest.py</read_first>
  <behavior>
    - Test 1: POST /auth/password with correct current_password and matching new passwords returns 303 redirect with msg= success param
    - Test 2: After password change, login with old password fails (401/re-render)
    - Test 3: After password change, login with new password succeeds (303 redirect)
    - Test 4: POST /auth/password with wrong current_password returns redirect with error= param containing "aktuell" (wrong current password)
    - Test 5: POST /auth/password with mismatched new_password and confirm_password returns redirect with error= param containing "stimmen nicht"
    - Test 6: POST /auth/password with new_password shorter than 8 chars returns redirect with error= param containing "mindestens"
    - Test 7: Unauthenticated POST to /auth/password returns 303 redirect to /login
    - Test 8: Teacher can change password (same endpoint works for all roles)
    - Test 9: Admin can change password (same endpoint works for all roles)
  </behavior>
  <action>
    **Step 1: Create tests/test_password.py** with all 9 test cases above. Use existing conftest fixtures:
    - `student_client` (authenticated as student1@test.com / studentpass)
    - `teacher_client` (authenticated as teacher@test.com / teacherpass)
    - `admin_client` (authenticated as admin@test.com / adminpass)
    - `test_client` (unauthenticated)
    - `db_session` for verifying password hash changed

    POST data fields: `current_password`, `new_password`, `confirm_password`
    Endpoint: `POST /auth/password`
    
    On success: redirect to role-based URL with `?msg=Passwort+erfolgreich+geaendert.`
    - admin -> `/admin`
    - teacher -> `/teacher`
    - student -> `/student`

    On error: redirect back to role-based URL with `?error=<message>`
    Error messages (German, per D-08):
    - Wrong current password: `Aktuelles Passwort ist falsch.`
    - Mismatch: `Neue Passwoerter stimmen nicht ueberein.`
    - Too short: `Neues Passwort muss mindestens 8 Zeichen lang sein.`

    **Step 2: Create POST /auth/password endpoint** in `app/routers/auth.py`:
    - Add import: `from app.dependencies import get_current_user`
    - Add import: `from app.services.auth import verify_password, get_password_hash` (get_password_hash already imported indirectly; add verify_password)
    - Endpoint signature:
      ```python
      @router.post("/auth/password")
      async def change_password(
          current_password: str = Form(...),
          new_password: str = Form(...),
          confirm_password: str = Form(...),
          user: User = Depends(get_current_user),
          db: Session = Depends(get_db),
      ):
      ```
    - Determine redirect base by user.role: admin -> "/admin", teacher -> "/teacher", student -> "/student"
    - Validate new_password length >= 8
    - Validate new_password == confirm_password
    - Validate verify_password(current_password, user.password_hash)
    - On validation failure: RedirectResponse to `{base}?error={message}`, status_code=303
    - On success: update user.password_hash = get_password_hash(new_password), db.commit(), redirect to `{base}?msg=Passwort+erfolgreich+geaendert.`
    - Per D-03: reuse existing verify_password() and get_password_hash() from auth service

    **Step 3: Create app/templates/_password_change.html** partial template:
    - A Jinja2 partial (not extending any base) containing an `<article>` with:
      - `<h2>Passwort aendern</h2>`
      - `<form method="post" action="/auth/password">`
      - Three labeled password inputs: current_password, new_password (minlength="8"), confirm_password
      - Submit button: "Passwort aendern"
    - Style with Pico CSS defaults (no custom CSS needed)
    - Per D-02: form fields are current password, new password, confirm new password

    **Step 4: Include partial in role dashboards** (per D-01 -- form lives ON the dashboard):
    - `app/templates/student.html`: Add `{% include "_password_change.html" %}` after the existing article
    - `app/templates/teacher_dashboard.html`: Add `{% include "_password_change.html" %}` after the lessons section (after the `{% endif %}` that closes the lessons block)
    - `app/templates/admin_base.html`: Add `{% include "_password_change.html" %}` after the `{% block admin_content %}{% endblock %}` line, before `{% endblock %}` (the content block end). This makes the form appear at the bottom of every admin page.
  </action>
  <verify>
    <automated>cd /home/danylo/Documents/Vorlesungen/SmartAttend && python -m pytest tests/test_password.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "change_password" app/routers/auth.py
    - grep -q "verify_password" app/routers/auth.py
    - grep -q "current_password" app/templates/_password_change.html
    - grep -q "_password_change.html" app/templates/student.html
    - grep -q "_password_change.html" app/templates/teacher_dashboard.html
    - grep -q "_password_change.html" app/templates/admin_base.html
    - grep -q "test_change_password" tests/test_password.py
    - python -m pytest tests/test_password.py -x passes all tests
  </acceptance_criteria>
  <done>All 9 password change tests pass. Students, teachers, and admins can change their own password from their dashboard page. Wrong current password, mismatched new passwords, and too-short passwords all show appropriate German error messages.</done>
</task>

</tasks>

<verification>
- All existing tests still pass: `python -m pytest tests/ -x`
- Password change tests pass: `python -m pytest tests/test_password.py -x -v`
- Password change form visible on student, teacher, and admin pages
</verification>

<success_criteria>
- POST /auth/password endpoint exists and handles all three validation cases
- Password change form partial exists and is included in all 3 role dashboards
- 9 tests cover happy path, validation errors, and multi-role usage
</success_criteria>

<output>
After completion, create `.planning/phases/11-password-management/11-01-SUMMARY.md`
</output>
